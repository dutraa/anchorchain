Metadata-Version: 2.4
Name: anchorchain-sdk-python
Version: 0.1.0
Summary: Minimal Python SDK for the AnchorChain HTTP API
Requires-Python: >=3.9
Description-Content-Type: text/markdown
Requires-Dist: requests<3,>=2.31.0

# AnchorChain Python SDK

A minimal Python client for the existing AnchorChain HTTP API.

## What it does

- checks node health
- creates chains
- writes entries
- fetches chains and entries
- verifies receipts

The SDK maps directly to the current HTTP API and does not invent protocol behavior.

## Local install

From the repo root:

```bash
cd sdk/python
python -m pip install -e .
```

## Local use

```python
from anchorchain import AnchorChainClient

client = AnchorChainClient(base_url="http://127.0.0.1:8081")
health = client.health()
print(health.status)
```

The client exposes Python-style methods:

- `health()`
- `create_chain(...)`
- `write_entry(...)`
- `get_chain(...)`
- `get_entries(...)`
- `get_entry(...)`
- `verify_receipt(...)`

For parity with the JavaScript SDK, camelCase aliases are also available:

- `createChain(...)`
- `writeEntry(...)`
- `getChain(...)`
- `getEntries(...)`
- `getEntry(...)`
- `verifyReceipt(...)`

## Run the included example

The example expects a running AnchorChain HTTP API. Local devnet works well:

```bash
make build
./bin/anchorchaind devnet
```

Then in another terminal:

```bash
cd sdk/python
python -m pip install -e .
python examples/basic.py
```

Environment variables used by the example:

- `ANCHORCHAIN_API` default: `http://127.0.0.1:8081`
- `ANCHORCHAIN_API_TOKEN` optional

## Local devnet troubleshooting

On local devnet, write requests may succeed before follow-up reads or receipts are available.

- `Missing Chain Head` usually means the chain is not readable from the API yet.
- `receipt not available` means the entry exists but receipt data is not ready yet.
- `Receipt creation error` can appear temporarily on local devnet while receipt generation lags or fails.
- network errors such as connection refusal usually mean the local API is not up yet.

`AnchorChainSDKError` preserves the raw response `status` and `body`, and marks common temporary states as `retriable`.

## Short usage example

```python
from anchorchain import AnchorChainClient

client = AnchorChainClient(base_url="http://127.0.0.1:8081")

chain = client.create_chain(
    ext_ids=["demo"],
    schema="json",
    payload={"hello": "anchorchain"},
)

print(chain.chain_id)
```
